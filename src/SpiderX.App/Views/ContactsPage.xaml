<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SpiderX.App.ViewModels"
             x:Class="SpiderX.App.Views.ContactsPage"
             x:DataType="vm:ContactsViewModel"
             Title="Contacts">

    <ContentPage.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0A0A1A" Offset="0.0" />
            <GradientStop Color="#0A1A2A" Offset="0.5" />
            <GradientStop Color="#1A0A2A" Offset="1.0" />
        </LinearGradientBrush>
    </ContentPage.Background>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Command="{Binding AddContactCommand}" />
        <ToolbarItem Text="Scan" Command="{Binding ScanQrCommand}" />
    </ContentPage.ToolbarItems>

    <RefreshView Command="{Binding RefreshCommand}" IsRefreshing="{Binding IsRefreshing}">
        <ScrollView>
            <VerticalStackLayout Spacing="16" Padding="16">

                <!-- Header -->
                <HorizontalStackLayout Spacing="12">
                    <Border WidthRequest="48" HeightRequest="48" StrokeThickness="0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#8B5CF6" Offset="0.0" />
                                <GradientStop Color="#EC4899" Offset="1.0" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="14" />
                        </Border.StrokeShape>
                        <Label Text="👥" FontSize="24" HorizontalOptions="Center" VerticalOptions="Center" />
                    </Border>
                    <VerticalStackLayout VerticalOptions="Center">
                        <Label Text="My Contacts" FontSize="22" FontAttributes="Bold" TextColor="#F8FAFC" />
                        <Label Text="{Binding Contacts.Count, StringFormat='{0} contacts'}" FontSize="13" TextColor="#64748B" />
                    </VerticalStackLayout>
                </HorizontalStackLayout>

                <!-- Pending Requests Section -->
                <Border StrokeThickness="0" Padding="20" IsVisible="{Binding PendingRequests.Count, Converter={StaticResource IntToBoolConverter}}">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#2A1A1A" Offset="0.0" />
                            <GradientStop Color="#1A1020" Offset="1.0" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="20" />
                    </Border.StrokeShape>
                    <Border.Shadow>
                        <Shadow Brush="#F59E0B" Offset="0,6" Radius="16" Opacity="0.2" />
                    </Border.Shadow>

                    <VerticalStackLayout Spacing="16">
                        <HorizontalStackLayout Spacing="12">
                            <Border WidthRequest="36" HeightRequest="36" StrokeThickness="0">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#F59E0B" Offset="0.0" />
                                        <GradientStop Color="#EF4444" Offset="1.0" />
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10" />
                                </Border.StrokeShape>
                                <Label Text="🔔" FontSize="18" HorizontalOptions="Center" VerticalOptions="Center" />
                            </Border>
                            <Label Text="Pending Requests" FontSize="18" FontAttributes="Bold" TextColor="#F8FAFC" VerticalOptions="Center" />
                            <Border BackgroundColor="#F59E0B" Padding="10,4" StrokeThickness="0">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10" />
                                </Border.StrokeShape>
                                <Label Text="{Binding PendingRequests.Count}" FontSize="12" FontAttributes="Bold" TextColor="White" />
                            </Border>
                        </HorizontalStackLayout>

                        <CollectionView ItemsSource="{Binding PendingRequests}">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="vm:ContactItem">
                                    <Border Margin="0,6" Padding="14" StrokeThickness="1" Stroke="#3A2A2A" BackgroundColor="#1A1018">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="14" />
                                        </Border.StrokeShape>
                                        <Grid ColumnDefinitions="Auto,*,Auto,Auto" ColumnSpacing="12">
                                            <!-- Avatar -->
                                            <Border WidthRequest="48" HeightRequest="48" StrokeThickness="0">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                        <GradientStop Color="#F59E0B" Offset="0.0" />
                                                        <GradientStop Color="#EF4444" Offset="1.0" />
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                                <Border.StrokeShape>
                                                    <RoundRectangle CornerRadius="14" />
                                                </Border.StrokeShape>
                                                <Label Text="?" FontSize="22" FontAttributes="Bold" TextColor="White" HorizontalOptions="Center" VerticalOptions="Center" />
                                            </Border>

                                            <!-- Info -->
                                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="4">
                                                <Label Text="{Binding DisplayName}" FontSize="15" FontAttributes="Bold" TextColor="#F8FAFC" />
                                                <Label Text="Wants to connect" FontSize="12" TextColor="#F59E0B" />
                                            </VerticalStackLayout>

                                            <!-- Accept button -->
                                            <Border Grid.Column="2" StrokeThickness="0" Padding="0">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                        <GradientStop Color="#22C55E" Offset="0.0" />
                                                        <GradientStop Color="#10B981" Offset="1.0" />
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                                <Border.StrokeShape>
                                                    <RoundRectangle CornerRadius="10" />
                                                </Border.StrokeShape>
                                                <Border.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ContactsViewModel}}, Path=AcceptRequestCommand}" CommandParameter="{Binding .}" />
                                                </Border.GestureRecognizers>
                                                <Label Text="✓ Accept" FontSize="13" FontAttributes="Bold" TextColor="White" Padding="14,10" />
                                            </Border>

                                            <!-- Reject button -->
                                            <Border Grid.Column="3" StrokeThickness="1" Stroke="#5A2A3A" BackgroundColor="#2A1A1E" Padding="0">
                                                <Border.StrokeShape>
                                                    <RoundRectangle CornerRadius="10" />
                                                </Border.StrokeShape>
                                                <Border.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ContactsViewModel}}, Path=RejectRequestCommand}" CommandParameter="{Binding .}" />
                                                </Border.GestureRecognizers>
                                                <Label Text="✗" FontSize="16" TextColor="#EF4444" Padding="12,8" HorizontalOptions="Center" />
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Border>

                <!-- Contacts Section -->
                <Border StrokeThickness="0" Padding="20">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#1A1A40" Offset="0.0" />
                            <GradientStop Color="#151530" Offset="1.0" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="20" />
                    </Border.StrokeShape>
                    <Border.Shadow>
                        <Shadow Brush="#8B5CF6" Offset="0,6" Radius="16" Opacity="0.2" />
                    </Border.Shadow>

                    <VerticalStackLayout Spacing="16">
                        <HorizontalStackLayout Spacing="12">
                            <Border WidthRequest="36" HeightRequest="36" StrokeThickness="0">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#8B5CF6" Offset="0.0" />
                                        <GradientStop Color="#EC4899" Offset="1.0" />
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10" />
                                </Border.StrokeShape>
                                <Label Text="👤" FontSize="18" HorizontalOptions="Center" VerticalOptions="Center" />
                            </Border>
                            <Label Text="All Contacts" FontSize="18" FontAttributes="Bold" TextColor="#F8FAFC" VerticalOptions="Center" />
                        </HorizontalStackLayout>

                        <CollectionView ItemsSource="{Binding Contacts}">
                            <CollectionView.EmptyView>
                                <Border Padding="30" BackgroundColor="#12121F" StrokeThickness="1" Stroke="#2A2A4A">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="14" />
                                    </Border.StrokeShape>
                                    <VerticalStackLayout Spacing="16" HorizontalOptions="Center">
                                        <Border WidthRequest="70" HeightRequest="70" StrokeThickness="0" HorizontalOptions="Center">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#1A2A4A" Offset="0.0" />
                                                    <GradientStop Color="#2A1A4A" Offset="1.0" />
                                                </LinearGradientBrush>
                                            </Border.Background>
                                            <Border.StrokeShape>
                                                <RoundRectangle CornerRadius="20" />
                                            </Border.StrokeShape>
                                            <Label Text="👥" FontSize="32" HorizontalOptions="Center" VerticalOptions="Center" />
                                        </Border>
                                        <Label Text="No contacts yet" FontSize="16" FontAttributes="Bold" TextColor="#F8FAFC" HorizontalTextAlignment="Center" />
                                        <Label Text="Tap + to add a contact" FontSize="13" TextColor="#64748B" HorizontalTextAlignment="Center" />
                                    </VerticalStackLayout>
                                </Border>
                            </CollectionView.EmptyView>

                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="vm:ContactItem">
                                    <SwipeView>
                                        <SwipeView.RightItems>
                                            <SwipeItems>
                                                <SwipeItem Text="Block" BackgroundColor="#EF4444"
                                                           Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ContactsViewModel}}, Path=BlockContactCommand}"
                                                           CommandParameter="{Binding .}" />
                                            </SwipeItems>
                                        </SwipeView.RightItems>

                                        <Border Margin="0,6" Padding="14" StrokeThickness="1" Stroke="#2A2A4A" BackgroundColor="#161630">
                                            <Border.StrokeShape>
                                                <RoundRectangle CornerRadius="14" />
                                            </Border.StrokeShape>
                                            <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="14">
                                                <!-- Avatar with online indicator -->
                                                <Grid WidthRequest="52" HeightRequest="52">
                                                    <Border WidthRequest="52" HeightRequest="52" StrokeThickness="0">
                                                        <Border.Background>
                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                <GradientStop Color="#7C3AED" Offset="0.0" />
                                                                <GradientStop Color="#EC4899" Offset="1.0" />
                                                            </LinearGradientBrush>
                                                        </Border.Background>
                                                        <Border.StrokeShape>
                                                            <RoundRectangle CornerRadius="14" />
                                                        </Border.StrokeShape>
                                                        <Border.Shadow>
                                                            <Shadow Brush="#7C3AED" Offset="0,3" Radius="8" Opacity="0.3" />
                                                        </Border.Shadow>
                                                        <Label Text="{Binding DisplayName, Converter={StaticResource InitialsConverter}}" FontSize="18" FontAttributes="Bold" TextColor="White" HorizontalOptions="Center" VerticalOptions="Center" />
                                                    </Border>
                                                    <!-- Online indicator -->
                                                    <Border WidthRequest="16" HeightRequest="16" StrokeThickness="2" Stroke="#161630"
                                                            HorizontalOptions="End" VerticalOptions="End" IsVisible="{Binding IsOnline}">
                                                        <Border.Background>
                                                            <SolidColorBrush Color="#22C55E" />
                                                        </Border.Background>
                                                        <Border.StrokeShape>
                                                            <RoundRectangle CornerRadius="8" />
                                                        </Border.StrokeShape>
                                                        <Border.Shadow>
                                                            <Shadow Brush="#22C55E" Offset="0,0" Radius="6" Opacity="0.8" />
                                                        </Border.Shadow>
                                                    </Border>
                                                </Grid>

                                                <!-- Name and status -->
                                                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="4">
                                                    <Label Text="{Binding DisplayName}" FontSize="16" FontAttributes="Bold" TextColor="#F8FAFC" />
                                                    <Label Text="{Binding StatusText}" FontSize="12" TextColor="#94A3B8" />
                                                </VerticalStackLayout>

                                                <!-- Actions -->
                                                <HorizontalStackLayout Grid.Column="2" Spacing="8" VerticalOptions="Center">
                                                    <Border WidthRequest="40" HeightRequest="40" StrokeThickness="1" Stroke="#3A3A5A" BackgroundColor="#1A1A2E">
                                                        <Border.StrokeShape>
                                                            <RoundRectangle CornerRadius="10" />
                                                        </Border.StrokeShape>
                                                        <Label Text="💬" FontSize="18" HorizontalOptions="Center" VerticalOptions="Center" />
                                                    </Border>
                                                    <Border WidthRequest="40" HeightRequest="40" StrokeThickness="1" Stroke="#3A3A5A" BackgroundColor="#1A1A2E">
                                                        <Border.StrokeShape>
                                                            <RoundRectangle CornerRadius="10" />
                                                        </Border.StrokeShape>
                                                        <Label Text="📞" FontSize="18" HorizontalOptions="Center" VerticalOptions="Center" />
                                                    </Border>
                                                </HorizontalStackLayout>
                                            </Grid>
                                        </Border>
                                    </SwipeView>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Border>

            </VerticalStackLayout>
        </ScrollView>
    </RefreshView>
</ContentPage>
